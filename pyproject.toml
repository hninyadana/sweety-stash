[project]
name = "repl-nlx-workspace"
version = "0.1.0"
description = "Improve financial tracking by enhancing API requests."
requires-python = ">=3.11"
dependencies = [
# Removed "flask" and "gunicorn". Added "streamlit" and necessary packages.
"streamlit>=1.0.0",
# Add any other core dependencies here (e.g., pandas, requests)
]

[tool.pdm.dev-dependencies]

